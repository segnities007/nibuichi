import 'package:flutter/material.dart';

class ItemShopUI extends StatelessWidget{
  const ItemShopUI({super.key});

  @override
  Widget build(context){
    return const Center(
      child: Text("item_shop"),
    );
  }
}